import React from 'react'

const MenuItem = ({item}) => {
  return (
    <li className='menu-item-container'>
        <p>{item.label}</p>
        <p>{item.label}</p>
    </li>
  )
}

export default MenuItem
